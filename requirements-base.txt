# Base requirements - Common dependencies used by all services
# This file contains ~80% of dependencies shared across API, Processor, and Crawler

# Core data processing
pandas>=2.0.0
numpy>=2.0.0  # Python 3.11+ supports numpy 2.x with all dependencies
pyarrow>=12.0.0  # For Parquet support
openpyxl>=3.0.0  # Excel export support

# Database
sqlalchemy>=2.0.0
alembic>=1.12.0
psycopg2-binary>=2.9.0  # PostgreSQL adapter for production
cloud-sql-python-connector[pg8000]>=1.11.0  # Google Cloud SQL connector

# HTTP and web utilities
requests>=2.28.0
urllib3>=1.26.0
beautifulsoup4>=4.11.0
lxml>=4.9.0
lxml_html_clean>=0.4.0  # Required for newspaper3k HTML cleaning
storysniffer>=1.0.9  # URL verification service (skops-based models)
selenium>=4.10.0  # Needed by tests
newspaper4k>=0.9.4.1  # Needed by tests
feedparser>=6.0.10  # Needed by tests
rapidfuzz>=3.0.0  # Needed by tests
cloudscraper>=1.2.71  # Needed by tests

# NLP and text processing
spacy>=3.8.0  # Python 3.11+ supports latest spacy with numpy 2.x
nltk>=3.8
textblob>=0.17.0
ftfy>=6.1.0
python-dateutil>=2.8.0
scikit-learn>=1.7.2,<1.8  # Compatible with StorySniffer 1.0.9+ skops models (model trained with 1.7.2)

# Testing
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-mock
requests-mock
nbformat
nbconvert
fastapi>=0.103.0  # Needed for test fixtures
httpx  # Needed for FastAPI TestClient

# Development
pre-commit>=3.3.0
black>=23.0.0
isort>=5.12.0
flake8>=6.0.0

# Utilities
click>=8.1.0
python-dotenv>=1.0.0
pydantic>=2.0.0
pyyaml>=6.0  # For Kubernetes manifest generation

# Observability and Monitoring
structlog>=24.0.0  # Structured logging
google-cloud-logging>=3.10.0  # Cloud Logging integration
google-cloud-monitoring>=2.20.0  # Cloud Monitoring metrics
