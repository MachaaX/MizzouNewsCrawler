# Example environment variables for local development and Kubernetes deployment

# Runtime
APP_ENV=local
LOG_LEVEL=INFO

# Database configuration
# Option A: use a full database URL (default for local development)
DATABASE_URL=sqlite:///data/mizzou.db
# Option B: compose a URL automatically from discrete settings (recommended for Kubernetes)
DATABASE_ENGINE=postgresql+psycopg2
DATABASE_HOST=
DATABASE_PORT=5432
DATABASE_NAME=
DATABASE_USER=
DATABASE_PASSWORD=
DATABASE_SSLMODE=
DATABASE_REQUIRE_SSL=false

# Cloud SQL Connector (replaces proxy sidecar - recommended for production)
# When enabled, connects directly to Cloud SQL without needing a sidecar container
USE_CLOUD_SQL_CONNECTOR=false
CLOUD_SQL_INSTANCE=  # Format: project:region:instance (e.g., my-project:us-central1:my-db)

# Telemetry API / services
TELEMETRY_URL=
TELEMETRY_HOST=
TELEMETRY_PORT=
TELEMETRY_SCHEME=http
TELEMETRY_USE_TLS=false
TELEMETRY_BASE_PATH=

# OAuth (optional)
OAUTH_PROVIDER=
OAUTH_CLIENT_ID=
OAUTH_CLIENT_SECRET=
OAUTH_REDIRECT_URI=
REQUEST_TIMEOUT=20
REQUEST_DELAY=1.0

# Kubernetes-specific context (optional)
KUBERNETES_NAMESPACE=

# Proxy Configuration
# See docs/PROXY_CONFIGURATION.md for details

# Master proxy provider switch
PROXY_PROVIDER=squid  # Options: squid, direct, standard, socks5, scraper_api, brightdata, smartproxy

# Squid proxy (default)
SQUID_PROXY_URL=http://squid.proxy.net:3128
SQUID_PROXY_USERNAME=
SQUID_PROXY_PASSWORD=

# Other proxy providers (optional)
STANDARD_PROXY_URL=
STANDARD_PROXY_USERNAME=
STANDARD_PROXY_PASSWORD=

SOCKS5_PROXY_URL=
SOCKS5_PROXY_USERNAME=
SOCKS5_PROXY_PASSWORD=

BRIGHTDATA_PROXY_URL=
BRIGHTDATA_USERNAME=
BRIGHTDATA_PASSWORD=
BRIGHTDATA_ZONE=residential

SCRAPERAPI_KEY=

SMARTPROXY_URL=
SMARTPROXY_USERNAME=
SMARTPROXY_PASSWORD=
